# copy to .env and fill in
SHEET_ID=your_google_sheet_id_here
SHEET_TAB=Sheet1
DAILY_CAP=130
POLL_SECONDS=180
DEVICE=DESKTOP
# secrets live in their own gitignored files instead of here:
#   .stitch_key       <- Stitch API key (one line)
#   .netlify_token    <- Netlify personal access token (one line)
#   .gcp_sa.json      <- Google service-account JSON key
